# Copy to .env and fill in your key (get one free at https://console.groq.com).
# The backend loads this file automatically on startup — no manual export needed.
GROQ_API_KEY=

# Optional: override the defaults below (only needed for non-standard local setups).
# PORT=8080
# ALLOWED_ORIGIN=http://localhost:5173
