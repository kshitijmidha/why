# Copy to .env to override the backend URL for local development.
# Leave empty (or delete the file) to use the default http://localhost:8080.
# On Vercel/Netlify, set VITE_API_BASE_URL in the dashboard instead —
# e.g. VITE_API_BASE_URL=https://why-backend.onrender.com
VITE_API_BASE_URL=
